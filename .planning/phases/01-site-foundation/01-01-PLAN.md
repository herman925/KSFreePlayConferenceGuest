---
phase: 01-site-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - styles/styles.css
  - pages/overview.html
  - pages/schedule.html
  - pages/activity_details.html
  - pages/resources.html
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Participant opens the site on a phone and sees a homepage with cards for Overview, Schedule, Activity Details, Resources/Downloads, and Surveys & Classroom"
    - "Every card on the homepage links to a corresponding page"
    - "Every sub-page has a back-to-home link visible at the top"
    - "No horizontal scrolling occurs on a 375px-wide viewport"
    - "Text and cards are legible without zooming (minimum 16px body text, 44px tap targets)"
  artifacts:
    - path: "index.html"
      provides: "Homepage with five navigation cards"
      contains: "dashboard-grid"
    - path: "styles/styles.css"
      provides: "Visual styles ported from organizer site, guest-specific overrides"
      contains: "--brand-navy"
    - path: "pages/overview.html"
      provides: "Overview stub page with back button"
      contains: "back-button"
    - path: "pages/schedule.html"
      provides: "Schedule stub page with back button"
      contains: "back-button"
    - path: "pages/activity_details.html"
      provides: "Activity Details stub page with back button"
      contains: "back-button"
    - path: "pages/resources.html"
      provides: "Resources stub page with back button"
      contains: "back-button"
  key_links:
    - from: "index.html .card[data-section]"
      to: "pages/*.html"
      via: "click handler in inline script"
      pattern: "window\\.location\\.href"
    - from: "pages/*.html"
      to: "index.html"
      via: "back-button anchor href"
      pattern: "href=\"\\.\\./index\\.html\""
    - from: "pages/*.html"
      to: "styles/styles.css"
      via: "link rel stylesheet"
      pattern: "href=\"\\.\\./styles/styles\\.css\""
---

<objective>
Port the visual shell from the organizer site and build a guest-facing homepage with five navigation cards plus four stub sub-pages, all wired together with back-navigation.

Purpose: Participants need to open the site on a phone and immediately see where everything is. This plan lays the HTML/CSS foundation — the navigable skeleton that Phase 2 will fill with content.
Output: index.html, styles/styles.css, and stub pages for overview, schedule, activity details, and resources.
</objective>

<execution_context>
@C:/Users/hkkchan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hkkchan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source site to reference (do not copy organizer-only pages):
- C:/Users/hkkchan/Downloads/KSFreePlayConference/index.html
- C:/Users/hkkchan/Downloads/KSFreePlayConference/styles/styles.css
- C:/Users/hkkchan/Downloads/KSFreePlayConference/pages/overview.html

<interfaces>
<!-- Key CSS classes from source site that must be preserved in styles/styles.css -->

From KSFreePlayConference/styles/styles.css:
- :root variables: --brand-navy (#2b3990), --brand-orange (#f99d33), --brand-pink (#f04e69), --brand-green (#8dbe50), --brand-yellow (#f4d036), --background-color (#f4f6f9), --card-shadow
- header: white, sticky, 4px rainbow top border (5-color gradient via ::before pseudo-element)
- .nav-wrapper: flex, align-items center, justify-content space-between, max-width 1200px
- .back-button: inline-flex, navy color, font-weight 600, hover changes background and color to orange
- .hero: centered, padding 3.5rem 1rem, h1 at 2.8rem navy font-weight 800
- .dashboard-grid: CSS grid, auto-fit minmax(280px, 1fr), gap 2rem, padding 2rem, max-width 1200px
- .card: white, padding 2rem, border-radius 12px, cursor pointer, hover translateY(-5px)
- @media (max-width: 768px): .dashboard-grid becomes single column, padding 1rem
- .content-wrapper: max-width 1200px, margin auto, padding 2rem (1rem on mobile)
- .page-header: centered, h1 2.2rem navy font-weight 800
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create styles/styles.css</name>
  <files>styles/styles.css</files>
  <action>
Read C:/Users/hkkchan/Downloads/KSFreePlayConference/styles/styles.css in full, then write styles/styles.css to the target directory keeping all of the following and stripping everything else:

KEEP: :root variables, * reset, body, header, header::before (rainbow bar), nav, .nav-wrapper, .back-button, .back-button:hover, .back-button i, .content-wrapper, .page-header and children, .hero and children, .dashboard-grid, .card and states, @media (max-width: 768px) blocks for dashboard-grid and content-wrapper only, .info-card and children, .details-grid, .detail-item, .btn, .btn-primary, .btn-secondary, .timeline-* styles.

STRIP (organizer-only): .person-selector, .tasks-container, .role-selector, .role-buttons, .role-btn, .role-tasks, .task-list, .task-item, .task-category*, .task-time*, .task-tag, .transport-table, .transport-check, .reporting-content, .info-card export variants, .export-*, .save-success @keyframes saveFeedback, .notification*.

At the end of the file add a "/* Guest site additions */" section containing:

  .surveys-card {
      background: linear-gradient(135deg, #2b3990 0%, #1a2570 100%);
  }
  .surveys-card i,
  .surveys-card h2,
  .surveys-card p {
      color: white;
  }
  .surveys-card:hover {
      background: linear-gradient(135deg, #1a2570 0%, #0f1a5e 100%);
      background-color: transparent;
  }

This makes the Surveys & Classroom card visually prominent on the homepage (supports SURV-04 in Phase 2).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const css=fs.readFileSync('C:/Users/hkkchan/Downloads/KSFreePlayConferenceGuest/styles/styles.css','utf8'); console.assert(css.includes('--brand-navy'),'missing brand vars'); console.assert(css.includes('.back-button'),'missing back-button'); console.assert(css.includes('.dashboard-grid'),'missing grid'); console.assert(!css.includes('.role-selector'),'role-selector not removed'); console.assert(!css.includes('reporting-content'),'reporting styles not removed'); console.assert(css.includes('.surveys-card'),'missing surveys-card'); console.log('CSS checks passed');"</automated>
  </verify>
  <done>styles/styles.css exists with all visual foundation classes, no organizer-only rules, and the .surveys-card guest addition at the end.</done>
</task>

<task type="auto">
  <name>Task 2: Create index.html (homepage with five cards)</name>
  <files>index.html</files>
  <action>
Create index.html at the root of the target directory. Use the source site's structure as a template but make these specific changes:

TITLE: "Free Play Case Conference — Participants"

HEADER: Replace the role selector nav with a simple site-title nav:
  &lt;header&gt;
    &lt;nav&gt;
      &lt;div class="nav-wrapper"&gt;
        &lt;span class="site-title"&gt;KeySteps@JC Free Play Conference&lt;/span&gt;
      &lt;/div&gt;
    &lt;/nav&gt;
  &lt;/header&gt;
Add .site-title inline style or in a style block: font-weight: 600; color: #2b3990; font-size: 1rem;

HERO SECTION: Keep same structure. Change h1 to "Conference Hub", keep both subtitle lines ("KeySteps@JC (Phase Two)" and "Free Play Case Conference"), keep the date-info line (January 4, 2025 (Saturday)).

DASHBOARD GRID: Exactly five cards in this order:
  1. data-section="overview"   — fa-info-circle      — "Overview"         — "Key objectives and conference info"
  2. data-section="schedule"   — fa-calendar-alt     — "Schedule"         — "Full programme and session times"
  3. data-section="activity-details" — fa-list-alt   — "Activity Details" — "Instructions for Activity 1 and 2"
  4. data-section="resources"  — fa-box-open         — "Resources & Downloads" — "Materials and downloadable files"
  5. data-section="surveys"    — fa-poll             — "Surveys & Classroom" — "Pre-test, post-test surveys and Google Classroom"

Card 5 must have class="card surveys-card" (adds the dark blue gradient from styles.css).

FOOTER: &lt;footer&gt;&lt;p&gt;KeySteps@JC Free Play Case Conference — January 4, 2025&lt;/p&gt;&lt;/footer&gt;

SCRIPT (inline, no external JS files):
  document.querySelectorAll('.card').forEach(card =&gt; {
    card.addEventListener('click', function() {
      const routes = {
        'overview': 'pages/overview.html',
        'schedule': 'pages/schedule.html',
        'activity-details': 'pages/activity_details.html',
        'resources': 'pages/resources.html',
        'surveys': 'pages/surveys.html'
      };
      const dest = routes[this.dataset.section];
      if (dest) window.location.href = dest;
    });
  });

All file references (CSS, fonts, Font Awesome) must use relative paths. No data fetching, no localStorage, no role logic.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('C:/Users/hkkchan/Downloads/KSFreePlayConferenceGuest/index.html','utf8'); console.assert(html.includes('dashboard-grid'),'missing grid'); console.assert(html.includes('surveys-card'),'missing surveys card class'); console.assert((html.match(/data-section=/g)||[]).length===5,'expected 5 cards, got '+(html.match(/data-section=/g)||[]).length); console.assert(html.includes('styles/styles.css'),'missing css link'); console.assert(!html.includes('roleSelect'),'role selector not removed'); console.assert(!html.includes('roles.json'),'roles.json fetch not removed'); console.log('index.html checks passed');"</automated>
  </verify>
  <done>index.html renders five navigation cards, no role selector, no broken references, correct relative paths to CSS and pages.</done>
</task>

<task type="auto">
  <name>Task 3: Create four stub sub-pages</name>
  <files>pages/overview.html, pages/schedule.html, pages/activity_details.html, pages/resources.html</files>
  <action>
Create four stub pages under pages/. Each page follows this exact structure:

  &lt;!DOCTYPE html&gt;
  &lt;html lang="en"&gt;
  &lt;head&gt;
      &lt;meta charset="UTF-8"&gt;
      &lt;meta name="viewport" content="width=device-width, initial-scale=1.0"&gt;
      &lt;title&gt;{PAGE TITLE} — Free Play Case Conference&lt;/title&gt;
      &lt;link rel="stylesheet" href="../styles/styles.css"&gt;
      &lt;link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&amp;display=swap" rel="stylesheet"&gt;
      &lt;link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"&gt;
  &lt;/head&gt;
  &lt;body&gt;
      &lt;header&gt;
          &lt;nav&gt;
              &lt;div class="nav-wrapper"&gt;
                  &lt;a href="../index.html" class="back-button"&gt;
                      &lt;i class="fas fa-arrow-left"&gt;&lt;/i&gt; Home
                  &lt;/a&gt;
              &lt;/div&gt;
          &lt;/nav&gt;
      &lt;/header&gt;
      &lt;main&gt;
          &lt;div class="page-header"&gt;
              &lt;h1&gt;&lt;i class="{ICON_CLASS}"&gt;&lt;/i&gt; {HEADING}&lt;/h1&gt;
              &lt;p class="subtitle"&gt;{SUBTITLE}&lt;/p&gt;
          &lt;/div&gt;
          &lt;div class="content-wrapper"&gt;
              &lt;p&gt;Content coming soon.&lt;/p&gt;
          &lt;/div&gt;
      &lt;/main&gt;
      &lt;footer&gt;&lt;p&gt;KeySteps@JC Free Play Case Conference — January 4, 2025&lt;/p&gt;&lt;/footer&gt;
  &lt;/body&gt;
  &lt;/html&gt;

Per-page values:

overview.html:
  PAGE TITLE: Overview
  ICON_CLASS: fas fa-info-circle
  HEADING: Overview
  SUBTITLE: Key objectives and conference information

schedule.html:
  PAGE TITLE: Schedule
  ICON_CLASS: fas fa-calendar-alt
  HEADING: Schedule
  SUBTITLE: Full programme and session times

activity_details.html:
  PAGE TITLE: Activity Details
  ICON_CLASS: fas fa-list-alt
  HEADING: Activity Details
  SUBTITLE: Instructions for Activity 1 and Activity 2

resources.html:
  PAGE TITLE: Resources and Downloads
  ICON_CLASS: fas fa-box-open
  HEADING: Resources and Downloads
  SUBTITLE: Materials and downloadable files

Note: surveys.html is NOT created here — it will be built in Phase 2 (SURV-01 through SURV-04).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pages=['overview','schedule','activity_details','resources']; pages.forEach(p=>{const path='C:/Users/hkkchan/Downloads/KSFreePlayConferenceGuest/pages/'+p+'.html'; const html=fs.readFileSync(path,'utf8'); console.assert(html.includes('../index.html'),'back link missing in '+p); console.assert(html.includes('back-button'),'back-button class missing in '+p); console.assert(html.includes('../styles/styles.css'),'css path wrong in '+p); console.assert(!html.includes('roleSelect'),'role selector in '+p);}); console.log('All stub pages OK');"</automated>
  </verify>
  <done>Four stub pages exist under pages/, each with a working back-to-home link using href="../index.html", correct relative CSS path, and the standard page-header structure. No broken references.</done>
</task>

</tasks>

<verification>
After all three tasks complete, verify the full site works as a navigable shell:

1. Open index.html in a browser (or via a local server: `npx serve C:/Users/hkkchan/Downloads/KSFreePlayConferenceGuest`).
2. Confirm five cards are visible with correct labels.
3. Confirm the Surveys & Classroom card has a dark blue background (visually distinct).
4. Click each of the first four cards and confirm navigation to the correct sub-page.
5. On each sub-page, confirm the "Home" back button returns to index.html.
6. Resize browser to 375px width and confirm no horizontal scrollbar appears on any page.

Automated structure check:
`node -e "const fs=require('fs'); ['index.html','pages/overview.html','pages/schedule.html','pages/activity_details.html','pages/resources.html'].forEach(f=>{fs.accessSync('C:/Users/hkkchan/Downloads/KSFreePlayConferenceGuest/'+f); console.log('EXISTS: '+f);}); console.log('All files present');"`
</verification>

<success_criteria>
- index.html has exactly five cards; no role selector; all links use relative paths
- styles/styles.css has no organizer-only rules; .surveys-card guest addition present
- Four stub pages exist under pages/; each has a back-button linking to ../index.html
- Site opens without JS errors on a plain file:// URL (no server required for navigation)
- Layout renders correctly at 375px width with no horizontal scroll
</success_criteria>

<output>
After completion, create `.planning/phases/01-site-foundation/01-01-SUMMARY.md` following the summary template at @C:/Users/hkkchan/.claude/get-shit-done/templates/summary.md.
</output>
